

services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - ess_data:/data
  frontend:
    build:
      context: .
      dockerfile: frontend/Dockerfile
    ports:
      - "3000:80"
    depends_on:
      - backend

volumes:
  ess_data:

